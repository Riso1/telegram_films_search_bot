# 🎬 Telegram-бот для поиска фильмов

Telegram-бот для поиска фильмов с использованием внешнего API  
**The Movie Database (TMDB)**.

Проект выполнен в рамках финальной работы курса  
**«Python-разработчик» (SkillBox)**.


---

## 🚀 Возможности бота

Бот позволяет:

- искать фильмы по названию;
- находить фильмы с высоким рейтингом;
- просматривать историю пользовательских запросов;
- работать через команды и кнопочное меню.

---

## 🤖 Команды бота

- `/start` — запуск бота и отображение меню;
- `/help` — справка по доступным командам;
- `/movie_search` — поиск фильма по названию;
- `/movie_by_rating` — поиск фильмов по рейтингу;
- `/history` — просмотр истории запросов.

Все команды также доступны через кнопочное меню (ReplyKeyboard).

---

## 🛠 Используемые технологии

- Python 3.10+
- pyTelegramBotAPI
- requests
- Peewee ORM
- SQLite
- The Movie Database API (TMDB)

---

## 🌐 Используемое API

### The Movie Database (TMDB)

Официальный API для работы с фильмами и сериалами.

Документация:  
https://developer.themoviedb.org

### Используемые endpoints

#### 🔹 Поиск фильма по названию

GET https://api.themoviedb.org/3/search/movie


Пример параметров запроса:
```python
params = {
    "api_key": TMDB_API_KEY,
    "query": "Matrix",
    "language": "ru-RU",
    "include_adult": False
}
```

Пример ответа (сокращённо):
```python
{
  "results": [
    {
      "title": "Матрица",
      "release_date": "1999-03-30",
      "vote_average": 8.2
    }
  ]
}
```

🔹 Поиск фильмов по рейтингу
GET https://api.themoviedb.org/3/discover/movie


Пример параметров запроса:

```python
params = {
    "api_key": TMDB_API_KEY,
    "sort_by": "vote_average.desc",
    "vote_average.gte": 7,
    "vote_count.gte": 100
}
```

💾 Хранение данных

Для хранения истории запросов используется:

- база данных SQLite;

- ORM Peewee.

Сохраняются следующие данные:

- ID пользователя Telegram;

- выполненная команда;

- параметры запроса;

- дата и время запроса.
